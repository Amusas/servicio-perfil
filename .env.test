# Database configuration (for testing with mocks)
DB_HOST=localhost
DB_PORT=5432
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=test_db

# JWT configuration
PUBLIC_KEY_PATH=/app/keys/public-key.pem

# Application
PORT=8087